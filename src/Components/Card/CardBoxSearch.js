import React from "react";

// Mui
import { Card, CardMedia } from "@mui/material";

const CardBoxSearch = ({ title }) => {
  return (
    <>
      <Card xs={6} md={4} lg={3}>
        {console.log(title)}
      </Card>
    </>
  );
};

export default CardBoxSearch;
