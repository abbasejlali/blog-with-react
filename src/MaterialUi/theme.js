import { createTheme } from "@mui/material";

const theme = createTheme({
  typography: {
    fontFamily: ["OpenSans", "Mikhak"],
  },
  bgColo: {
    themeKey: "#f2f2f2",
  },
});

export { theme };
